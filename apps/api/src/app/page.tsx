export default function ApiHomePage() {
  return (
    <main style={{ fontFamily: 'sans-serif', padding: '2rem' }}>
      <h1>HSS API Service</h1>
      <p>Use /api/health/live and /api/health/ready for health checks.</p>
    </main>
  );
}
